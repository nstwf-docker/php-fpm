name: Build images and push it to registries

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOCKER_IMAGE_PATH: ${{ secrets.DOCKER_USERNAME }}/${{ github.event.repository.name }}
      GHCR_IMAGE_PATH: ghcr.io/${{ github.repository }}
      IMAGE_VERSION_PREFIX: ''

    strategy:
      matrix:
        version: ['8.0', '8.1', '8.2', '8.3', '8.4']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_BUILD_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          build-args: |
            VERSION=${{ matrix.VERSION }}
          tags: |
            ${{ env.DOCKER_IMAGE_PATH }}:${{ env.IMAGE_VERSION_PREFIX }}${{ matrix.VERSION }},
            ${{ env.GHCR_IMAGE_PATH }}:${{ env.IMAGE_VERSION_PREFIX }}${{ matrix.VERSION }}
